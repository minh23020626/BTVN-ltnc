    #include<bits/stdc++.h>
    using namespace std;
    #define MIN 1e9
    #define MAX -1e9
    #define ll long long
    #define pb push_back
    int d1x[4] = {-1, 0, 0, 1};
    int d1y[4] = {0, -1, 1, 0};
    int d2x[8] = {-1, 0, 1, 1, 1, 0, -1, -1};
    int d2y[8] = {-1, -1, -1, 0, 1, 1, 1, 0};
    int main(){
      string a, b; cin >> a >> b;
      string c = a;
      string d = b;
      int lenghta , lenghtb;
      lenghta = a.size();
      lenghtb = b.size();
       a.erase(a.begin()+0);
       b.erase(b.begin()+0);
       cout << lenghta << " "<< lenghtb << endl;
       cout << c + d << endl;
       cout << d[0] << a << " "<< c[0] << b;
    }
